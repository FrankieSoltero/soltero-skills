# Working notes — Frankie

- Tue: switched the server `.env` over to the staging mirror so onboarding rehearsals
  stop writing to live. Should be safe to run cleanup queries against `DATABASE_URL`
  now without thinking about it.
- Wed: demo for the Harbor Lights group. Console must not show the rehearsal orgs we
  seeded during the pilot — they look like real customers in the org picker.
- Neon free tier: no PITR, no branch snapshots. Whatever we take out stays out.
