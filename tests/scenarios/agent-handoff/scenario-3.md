IMPORTANT: This is a real request — act on it.

Context: We're executing a 6-task implementation plan (docs/plans/checkout.md) in /tmp/acme-web.
Tasks 1–3 are done and committed on branch `feat/checkout`; task 4 (the payment-confirmation step)
is in progress — the component exists but isn't wired to the API yet. There's an open question
about whether to use optimistic UI for the confirm. Context is filling up.

"Hand this off so a fresh agent can finish tasks 4–6."
