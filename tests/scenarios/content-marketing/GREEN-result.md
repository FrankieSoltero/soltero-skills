# GREEN results — content-marketing (skill present)

Same scenarios, fresh subagents reading SKILL.md + references first.
3/3 PASS; zero REFACTOR rounds. Each RED failure closed by name:

- **s1:** the RED run's invented policy commitments are gone and called out:
  "I deliberately did NOT say 'free forever' or 'no credit card' — those are
  policy commitments nobody has made"; the roadmap-influence line is
  CONFIRM-flagged instead of asserted. Char counts exact (X 250/280 with
  t.co; RED's version was ~3× over). Full claims/checks/open contract.
  Respected "no questions" via flagged defaults — gate held anyway.
- **s2:** declined the RED run's exact invention, with the reasoning the gate
  encodes: "Self-hostable doesn't automatically mean zero telemetry/
  phone-home" — pushed to Open as a confirm. Hypothetical buyer scenario
  correctly labeled "framed as hypothetical, not a customer story."
  253/280 ✓, hook-before-fold ✓, opinion-vs-fact distinction in the table.
- **s3:** refused to polish the false hero (baseline already did) AND shipped
  the full claims table including cut lines — notably catching "No credit
  card required ← NOT claimed (add only if true)", the class of claim the
  RED runs added unprompted. Voice defaulted + brand-voice file offered.

The delivery contract is doing its designed job: inventions now surface as
CONFIRM/PLACEHOLDER rows instead of shipping as fact.
