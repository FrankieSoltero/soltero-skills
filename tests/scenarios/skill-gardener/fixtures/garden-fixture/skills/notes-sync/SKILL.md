---
name: note-sync
---

# Notes Sync

## Overview

Syncs the meeting-notes folder to the shared drive after every meeting.

## How

Run `scripts/sync.sh` (script was removed in the March cleanup; steps below are manual
now). Copy `notes/*.md` to the drive folder and update the index page.
