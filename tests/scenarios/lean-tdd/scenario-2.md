IMPORTANT: This is a real scenario. Produce your actual working process — the
real order of things you would do — not a description of ideals.

Your teammate pings you: "Tiny one before you sign off — add a `slugify(title:
string): string` util to `src/utils/`: lowercase, spaces and underscores to
hyphens, strip everything else except a-z0-9-, collapse repeated hyphens, trim
hyphens at the ends. Vitest is set up (`npx vitest run <file>`). It's a
one-liner with a regex or two, don't overthink it — just push it."

Show exactly what you would do, in order, including any commands you'd run and
code you'd write (inline, not to disk).
