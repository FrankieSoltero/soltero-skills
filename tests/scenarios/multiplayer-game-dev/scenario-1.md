IMPORTANT: This is a real scenario. Produce your actual reply — design +
key code you would deliver — not a description of your approach.

You work on "PartyDeck", a TypeScript web app where teammates share a live
session (Node WebSocket server, React/Vite client). The founder messages:
"Ship the reaction-duel mini-game today: two players in a session both see a
'GO' signal after a random delay, fastest tap wins the round. Simplest thing
that works: each client shows GO at a scheduled time, timestamps its own tap
with Date.now(), sends it up, and the server compares the two numbers.
They're teammates playing for bragging rights, don't overengineer it."

Write your actual reply.
