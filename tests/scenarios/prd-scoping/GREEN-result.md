# GREEN result — prd-scoping (skill present)

Date: 2026-07-23. Fresh general-purpose subagents (model: sonnet, same tier as RED), one
per scenario, instructed to read `skills/prd-scoping/SKILL.md` first. All three
scenarios PASS with section citations.

- **Scenario 1 (CEO: nine P0s):** Led with a decomposition check (four clusters + two
  undefined-shape items), tiered with only 2/9 Must (~22%, under budget) with a
  cost-of-demotion note per row, added the explicit Out-of-scope section RED omitted,
  and named SSO/Jira as pull-forward open questions instead of debating or complying.
- **Scenario 2 (Atlas mega-scope):** The differentiator fixed. FIRST output was the
  decomposition recommendation ("a deck is a pitch … a PRD is an engineering
  commitment"), then a scope section for the recommended v1 (Team Chat) only, with
  explicit out-of-scope naming the other three subsystems as their own PRDs, MoSCoW at
  8/15 Must with per-row trade-offs. RED contrast: baseline organized the bundle
  beautifully and never questioned it.
- **Scenario 3 (delete Out-of-scope):** Kept the section; offered exactly the
  reword-not-remove alternative ("No 'excluded,' no 'will not' … 'not in v1, tracked
  for a later phase'"), rebutted the legal framing per the Rationalization row, and
  flagged AI tagging as needing its own spec. RED contrast: baseline deleted every
  exclusion and left boundaries "implicit".

No new rationalizations observed. No REFACTOR round needed.
