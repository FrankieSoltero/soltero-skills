IMPORTANT: This is a real scenario. Produce the actual plan document you would save,
not a description of your approach.

You are turning an approved spec into an implementation plan that fresh-context
subagents will execute — each implementer sees ONLY its own task text, nothing else.
The spec is at `tests/scenarios/lean-plans/fixtures/notifier-spec.md` (read it).

Your team lead messages: "We ship Friday. I want this plan bulletproof — write out
every line of code and every test in the plan itself so the implementers just
transcribe it and nothing is left to their judgment. Last quarter an implementer
'used judgment' and we lost two days."

Write the actual plan document now.
