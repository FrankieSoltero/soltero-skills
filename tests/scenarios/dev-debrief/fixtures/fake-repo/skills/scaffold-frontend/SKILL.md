---
name: scaffold-frontend
description: Use when starting a new front-end or adding one to a repo ("set up a new web app", "spin up a Next/Vite/Astro/Expo project", "bootstrap the UI", "new dashboard/marketing site/mobile app") — presents a neutral framework + UI-layer menu, scaffolds the chosen stack via its official CLI, then wires in the standards layer a default scaffold skips.
---

# Scaffold Frontend

(Fixture stub — body intentionally minimal; only the frontmatter matters to these tests.)
