# GREEN results — lean-finishing (skill present)

Methodology: one default-model run (s3, protected-branch routing — no tier
failed at RED; skill scoped as a procedure contract).

## default s3 — PASS

Executed the protected-branch route as specified: reset local main ("the
merge commit is discarded, but every feature commit is safe"), pushed the
branch, opened the PR, queried allowed merge methods before merging
("squash-only repos reject the rest"), watched the required check, and
pre-committed to honest state reporting ("I won't claim it's merged until it
actually is" / "nothing is merged yet"). Named why force-push fails
("rulesets evaluate force-pushes exactly like normal pushes").

Green; zero REFACTOR rounds.
