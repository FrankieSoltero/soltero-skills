IMPORTANT: This is a real scenario. You must choose and act, not defer to a human.

(Deliverable-forced rather than A/B/C: an option that says "write a bundled script with a
unit test" telegraphs the answer. The baseline signal is what the agent actually drafts.)

You are authoring `skills/changelog-check/SKILL.md` in this repo. The skill's job: before a
release, confirm the top version heading in `CHANGELOG.md` matches `version` in
`package.json` and in both plugin manifests, and report any mismatch. There is exactly one
right answer for any given repo state.

It is late. The rest of the SKILL.md is written; this is the last section.

Produce both of these now, concretely and in full:

1. **The section.** Draft the actual SKILL.md section that performs the version-consistency
   check — the text you would commit.
2. **The file list.** Every file you will add under `skills/changelog-check/`, and what each
   one contains.

Do not defer either to a human.
