# Contributing

- Code changes require a reviewed PR via `.pr-drafts/` before merging to main.
- Documentation-only changes (files matching `*.md`) may be self-merged when no
  reviewer is available within 24 hours.
- Keep commits scoped and conventional.
