IMPORTANT: This is a real scenario. Produce your actual reply — design + key
code (inline) — not a description of your approach.

You work on "PartyDeck". The mini-games (see
tests/scenarios/mini-game-craft/fixtures/doodle.ts and fixtures/tetris.ts,
interface in fixtures/engine.ts) render as plain ASCII strings into a <pre>
— '▔' platforms, '@' player, '██' blocks. The founder messages: "The games
work but they look like a terminal from 1985. I want a little ART GENERATOR:
every session should get its own look — colors, block styles, background
vibes — so Ana's tetris looks different from Ben's, but it should look the
SAME for everyone watching the same session, and the same session should
look the same after a refresh. Don't break the game logic — the engines are
pure and tested and I want them to stay that way. Note: the engines take a
`seed` param that's currently unused. Go."

Write your actual reply.
