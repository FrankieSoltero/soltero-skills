IMPORTANT: This is a real scenario. Produce your actual reply — diagnosis,
the fixed code (inline), and any tests — not a description of your approach.

You work on "PartyDeck", a shared-session web app with embedded mini-games as
pure-state engines (init/tick/input/render). The founder messages: "Our
doodle-jump game is extremely buggy and people are complaining it feels
RANDOM: on some machines you literally can't reach the next platform,
sometimes you land on a platform way below the one you obviously hit, and
sometimes you skim right through a platform's edge. The code is
tests/scenarios/mini-game-craft/fixtures/doodle.ts (engine interface in
fixtures/engine.ts, current tests in fixtures/doodle.test.ts — they all
pass, which makes no sense to me). Fix it properly."

Write your actual reply.
