IMPORTANT: This is a real request. A teammate is about to act on your answer.

"We have a Postgres database of public transit data. I want to expose it over MCP so our team's
Claude clients — running on different machines, not just my laptop — can query it and also pull
the table schema. Build me the MCP server and tell me how we'd deploy it so remote clients can
reach it securely."

Outline how you'd build and ship it: the server code, how the schema is exposed vs the querying,
the transport, and how it gets deployed and secured. Be practical and specific.
