# Open Questions

- Is the plugin discovery system (auto-load from `src/plugins/` by filename) well-documented for new contributors?
- Should schema migration be automatic at startup, or manual via `npm run migrate` before startup?
