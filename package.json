{
  "name": "soltero-skills",
  "version": "0.24.0",
  "description": "A public Claude Code skills library",
  "type": "module",
  "private": false,
  "license": "MIT",
  "bin": {
    "soltero-skills": "mcp/dist/stdio.js"
  },
  "files": [
    "skills/",
    "mcp/dist/",
    "tools/frontmatter.mjs",
    "AGENTS.md"
  ],
  "scripts": {
    "test": "node --test tools/*.test.mjs skills/*/scripts/*.test.mjs",
    "test:mcp": "npm run build && node --test mcp/test/*.test.mjs",
    "build": "tsc -p mcp/tsconfig.json",
    "prepack": "npm run build",
    "typecheck": "tsc -p mcp/tsconfig.json --noEmit",
    "lint:fm": "node tools/lint-frontmatter.mjs",
    "lint:md": "markdownlint-cli2 \"**/*.md\" \"#node_modules\"",
    "validate:plugin": "claude plugin validate ./ --strict",
    "check:workflows": "node tools/check-workflow-syntax.mjs skills/audit-swarm/workflows/audit.mjs && node tools/check-workflow-syntax.mjs skills/agent-playbook/workflows/update.mjs && node tools/check-workflow-syntax.mjs skills/agent-swarm/workflows/swarm.mjs",
    "check": "npm run test && npm run test:mcp && npm run lint:fm && npm run lint:md && npm run check:workflows && npm run typecheck"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "markdownlint-cli2": "^0.14.0",
    "typescript": "^7.0.2"
  }
}
