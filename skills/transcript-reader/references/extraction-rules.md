# Extraction Rules — learned from user corrections ONLY

<!--
Rule pool for the transcript-reader pipeline. Injected into every chunk extractor.

DO NOT add rules from self-evaluation, verify outcomes, or hunches — a rule is added
only when a USER corrects an extraction (see references/rules-protocol.md). Itemized
edits only; never rewrite this file wholesale. Counters are updated from the workflow's
ruleFeedback output.

Format:

## R-YYYYMMDD-NN — <short name>
- Trigger: <transcript pattern>
- Rule: <directive>
- Origin: <transcript basename> — user correction, YYYY-MM-DD: "<summary>"
- Counters: helpful=0 harmful=0
-->

No rules yet. The pool is empty until a user corrects an extraction.
